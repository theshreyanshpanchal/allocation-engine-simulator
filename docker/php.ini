; Hardening for the public demo container. APP_DEBUG=false already keeps
; Laravel's own error pages generic; this covers the PHP-engine layer
; underneath it, which Laravel's exception handler doesn't touch.
expose_php = Off
display_errors = Off
display_startup_errors = Off
log_errors = On
